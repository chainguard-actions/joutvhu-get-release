name: Test prerelease input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        continue-on-error: true
        with:
          owner: actions
          repo: checkout
          latest: 'true'
          prerelease: 'true'
          throwing: 'false'

      - name: Assert action ran with prerelease input (outputs set, or failed gracefully)
        env:
          OUTCOME: ${{ steps.action.outcome }}
          TAG: ${{ steps.action.outputs.tag_name }}
          IS_PRERELEASE: ${{ steps.action.outputs.prerelease }}
        run: |
          echo "outcome: $OUTCOME"
          echo "tag_name: $TAG"
          echo "prerelease: $IS_PRERELEASE"
          if [ "$OUTCOME" = "success" ]; then
            if [ -z "$TAG" ]; then
              echo "FAIL: action succeeded but tag_name output is empty"
              exit 1
            fi
            echo "PASS: got tag_name='$TAG' prerelease='$IS_PRERELEASE'"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (API call failed under harness)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
